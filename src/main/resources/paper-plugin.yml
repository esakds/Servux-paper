name: Servux-paper
version: ${version}
main: com.codexmc.servuxcompat.PaperServuxCompatPlugin
description: Servux litematics compatibility layer for Litematica Easy Place v3 on Paper.
api-version: "1.21"
folia-supported: true
dependencies:
  server:
    ProtocolLib:
      load: BEFORE
      required: true
      join-classpath: true
